# note

1. Require a connection string type POOLER to support IPV4 runner.
2. Action node24 key is not "uses" is "using" on action.yaml
3. Supabase password should be simple without special characters (if not eant parse into a string specials)
4. Core debug ... dont show by default, better use core.info()